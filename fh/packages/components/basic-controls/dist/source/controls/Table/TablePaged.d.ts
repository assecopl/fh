import { Table } from "./Table";
import { HTMLFormComponent } from "fh-forms-handler";
declare class TablePaged extends Table {
    private pageSizeSelect;
    private sortedBy;
    private paginator;
    private pageInfo;
    private pageSize;
    private sortDirection;
    private readonly onPageChange;
    private readonly totalPages;
    private readonly totalRows;
    private currentPage;
    private readonly paginatorOffset;
    private pageChangeListeners;
    constructor(componentObj: any, parent: HTMLFormComponent);
    create(): void;
    pageSizeSelectEvent(event: any): void;
    update(change: any): void;
    onPaginatorClick(event: any): void;
    cleanUpSortableComponents(): void;
    buildPageInfo(): HTMLSpanElement;
    buildPaginator(): HTMLElement;
    changeSort(newSortedBy: any, newSortDirection: any): void;
    toInt: (stringOrInt: any) => any;
    extractChangedAttributes(): {};
    setAccessibility(accessibility: any): void;
    destroy(removeFromParent: boolean): void;
}
export { TablePaged };
