package pl.fhframework.model.forms.attribute;

/**
 * Option to align component vertically relative to parent element. Available values: top, middle, bottom.
 */
public enum ElementsVerticalAlign {
    TOP, MIDDLE, BOTTOM
}
